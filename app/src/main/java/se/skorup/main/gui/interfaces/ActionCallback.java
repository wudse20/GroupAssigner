package se.skorup.main.gui.interfaces;

/**
 * The interface used as a callback from
 * an action.
 * */
@FunctionalInterface
public interface ActionCallback
{
    /** The callback. */
    void callback();
}
